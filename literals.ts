let weekends: "Saturday" | "Sunday"
// now this variable can take only these two values, not other than that

// weekends = "Friday"; // throws error

let rgb: "red" | "green" | "blue";
// rgb = "black" // throws an error

let bool: true | false

// bool = 23 // throws an error