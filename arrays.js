"use strict";
const nums = [1, 2, 3, 4, 5]; // array of type number
const strNums = ["one", "two", "three"]; // array of type string
// nums.push("six") // throws an error bcz of type mismatch
console.log(nums);
console.log(strNums);
const foods = ["pizza", "burger", "pasta"]; // another type of initialisation
console.log(foods);
