const nums: number[] = [1,2,3,4,5] // array of type number

const strNums: string[] = ["one", "two", "three"] // array of type string

// nums.push("six") // throws an error bcz of type mismatch

console.log(nums);
console.log(strNums);

const foods: Array<string> = ["pizza", "burger", "pasta"] // another type of initialisation

console.log(foods);
